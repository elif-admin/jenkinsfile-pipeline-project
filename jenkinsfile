pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Clarusway_Way to Reinvent Yourself"
                sh 'echo usi shell within Jenkinsfile'
                echo 'not using shell in the Jenkinsfile'
            }
        }
    }
}